# Optional during local development; required on Render.
APP_PASSWORD=choose-a-long-club-password

# Required for automatic engraving reading. Images still save without it.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6-terra

# Local default is ./data-store. Render sets this to its persistent disk.
DATA_PATH=
